import { View } from 'react-native';

const separatorStyle = {
  height: 1,
  backgroundColor: 'lightgray',
};

const ListSeparator = () => <View style={separatorStyle}></View>;

export default ListSeparator;